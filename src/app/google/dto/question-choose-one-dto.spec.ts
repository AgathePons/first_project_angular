import { QuestionChooseOneDto } from './question-choose-one-dto';

describe('QuestionChooseOneDto', () => {
  it('should create an instance', () => {
    expect(new QuestionChooseOneDto()).toBeTruthy();
  });
});
